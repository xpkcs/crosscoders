defaults:
  - config
  - runner: data
  - _self_



runner:

  stage: data

  input_name: ln2.normalized
  output_name: mlp_out

  batch_size: 8
  max_tokens: 1000

  # dataset:
  #   slice: validation